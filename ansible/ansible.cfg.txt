
#Recommend setting in /etc/ansible/ansible.cfg

[NBNCO\blaineparris@svdam3100001ee ~]$ grep -i "host_key"  /etc/ansible/ansible.cfg
host_key_checking = False

[NBNCO\blaineparris@svdam3100001ee ~]$ cat  ~/.ansible.cfg
# Local config file for ansible -- https://ansible.com/
# ===============================================

# nearly all parameters can be overridden in ansible-playbook
# or with command line flags. ansible will read ANSIBLE_CONFIG,
# ansible.cfg in the current working directory, .ansible.cfg in
# the home directory or /etc/ansible/ansible.cfg, whichever it
# finds first

[defaults]
host_key_checking=false
